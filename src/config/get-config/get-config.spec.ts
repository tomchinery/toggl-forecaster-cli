describe('getConfig', (): void => {
  it('should get the config from the users home directory', (): void => {
    pending();
  });

  it('should error gracefully if the config file does not exist', (): void => {
    pending();
  });

  it('should validate that the config is correct', (): void => {
    pending();
  });
});
